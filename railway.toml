# Railway Configuration for WillFlow CRM
# Atualizado: 01/01/2026
#
# Note: Environment variables like SMTP_PASSWORD should be configured
# in Railway's dashboard, not in this file. They are injected at runtime
# for security. Nixpacks may show warnings about these variables during
# build, but they are handled correctly by Railway.

[build]
builder = "NIXPACKS"

[build.nixpacksPlan.phases.setup]
nixPkgs = ["nodejs-20_x", "bun", "openssl"]

[build.nixpacksPlan.phases.install]
cmds = [
  "bun install --no-save"
]

[build.nixpacksPlan.phases.build]
cmds = [
  "bunx prisma generate",
  "bunx prisma db push --accept-data-loss || echo 'DB migration skipped - using mock data fallback'",
  "bun run build"
]

[start]
cmd = "bun run start"

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
NODE_ENV = "production"
